


# library(DBI)
# library(tidyverse)
# # load("../downloads/basic.Rdata")
#
# if(!exists("../../database/ftf.db")){
#   ftfdb <- dbConnect(RSQLite::SQLite(), "../../database/ftf.db")
# }
#
# RSQLite::dbWriteTable(conn = ftfdb, name = "full_export", value = df, overwrite = T)
#
#
#
# dbDisconnect(ftfdb)
